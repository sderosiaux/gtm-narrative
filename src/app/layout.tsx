import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Northbridge Bank | A Streaming Data Journey",
  description: "A story about scattered streams, smart people, and the journey to a platform that actually works. Follow Emma and Daniel as they transform Kafka chaos into trusted data products.",
  openGraph: {
    title: "Northbridge Bank | A Streaming Data Journey",
    description: "A story about scattered streams and the journey to a platform that works.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
